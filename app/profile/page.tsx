import { chatGPTSignInPath, chatGPTSignOutPath, getChatGPTUser } from '../chatgpt-auth';
export const dynamic='force-dynamic';
export default async function Profile(){const user=await getChatGPTUser();if(!user)return <main className="formPage"><a className="brand" href="/"><span>S</span> Skillshot</a><section className="formCard auth"><p className="eyebrow">WELCOME TO SKILLSHOT</p><h1>Your work deserves a home.</h1><p>Sign in securely to upload screenshots, react, comment, and shape your profile. Your ChatGPT account provides a verified email, so there is no extra password to store or verification link to chase.</p><a className="primary" href={chatGPTSignInPath('/profile')}>Sign in with ChatGPT →</a></section></main>;return <main className="formPage"><a className="brand" href="/"><span>S</span> Skillshot</a><section className="formCard"><p className="eyebrow">YOUR PROFILE · EMAIL VERIFIED ✓</p><h1>{user.displayName}</h1><form action="/api/profile" method="post"><label>Display name<input name="displayName" defaultValue={user.displayName}/></label><label>Username<input name="username" defaultValue={user.email.split('@')[0]}/></label><label>Bio<textarea name="bio" placeholder="What do you make?"/></label><label>Website<input name="website" type="url" placeholder="https://"/></label><button className="primary">Save profile</button></form><a className="quietLink" href={chatGPTSignOutPath('/')}>Sign out</a></section></main>}
