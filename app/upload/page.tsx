'use client';
import { FormEvent, useState } from 'react';
export default function Upload(){const[preview,setPreview]=useState('');const[status,setStatus]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Uploading…');const r=await fetch('/api/posts',{method:'POST',body:new FormData(e.currentTarget)});setStatus(r.ok?'Your shot is live!':'Please sign in and check your image.');}return <main className="formPage"><a className="brand" href="/"><span>S</span> Skillshot</a><section className="formCard"><p className="eyebrow">SHARE YOUR WORK</p><h1>Put your skills in the frame.</h1><form onSubmit={submit}><label className="drop">{preview?<img src={preview} alt="Selected preview"/>:<><b>Drop your screenshot here</b><small>PNG, JPG, WebP or GIF · up to 10 MB</small></>}<input required name="image" type="file" accept="image/png,image/jpeg,image/webp,image/gif" onChange={e=>{const f=e.target.files?.[0];if(f)setPreview(URL.createObjectURL(f))}}/></label><label>Title<input required name="title" maxLength={100} placeholder="What did you make?"/></label><label>Description<textarea name="description" maxLength={1000} placeholder="Tell us about the idea or process"/></label><label>Skills / tags<input name="tags" placeholder="UI Design, React, Illustration"/></label><button className="primary" type="submit">Publish shot →</button><p role="status">{status}</p></form></section></main>}
