export function StaffSkeleton({ rows = 3 }: { rows?: number }) { return <div className="staffSkeletons" role="status" aria-label="Loading staff data">{Array.from({length:rows},(_,i)=><div className="staffSkeleton" key={i}><span/><span/><span/></div>)}</div>; }
export function StaffError({ retry }: { retry: () => void }) { return <div className="staffEmpty" role="alert"><h2>Something went wrong.</h2><p>We couldn’t load this information.</p><button type="button" onClick={retry}>Try again</button></div>; }
export function StaffEmpty({ title, children }: { title: string; children?: React.ReactNode }) { return <div className="staffEmpty"><span aria-hidden="true">✓</span><h2>{title}</h2><p>{children || 'Everything is caught up.'}</p></div>; }
export function StaffStatus({ value }: { value: string }) { return <span className={`staffStatus status${value}`} >{({HIGH:'High confidence',PENDING:'Pending review',VISIBLE:'Approved'} as Record<string,string>)[value] || value.toLowerCase().replaceAll('_',' ')}</span>; }
