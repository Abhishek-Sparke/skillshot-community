import Link from 'next/link';import { requirePanel } from '../../../lib/authz';import { getReadyDb } from '../../../lib/db';
export default async function Page(){const principal=await requirePanel('admin');if(!principal.permissions.includes('audit.view'))return <main className="staffPage shell"><h1>Audit log unavailable</h1><Link href="/admin">Back</Link></main>;const logs=await(await getReadyDb()).query(`SELECT l.*,u.display_name,u.username FROM audit_logs l LEFT JOIN users u ON u.id=l.actor_id ORDER BY l.created_at DESC LIMIT 100`);return <main className="staffPage shell"><nav className="staffNav"><Link className="brand" href="/"><span>S</span> Skillshot</Link><Link href="/admin">Dashboard</Link></nav><header><p className="eyebrow">ACCOUNTABILITY</p><h1>Audit log</h1><p>Permanent records of sensitive staff actions.</p></header><div className="staffTable">{logs.map(log=><article key={log.id}><div><b>{String(log.action).replaceAll('_',' ')}</b><span>{log.target_type} · {log.target_id}</span></div><div><small>{log.username?`@${log.username}`:'System'} · {new Date(log.created_at).toLocaleString()}</small></div></article>)}</div></main>}
