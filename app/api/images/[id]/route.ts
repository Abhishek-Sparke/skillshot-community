import { env } from 'cloudflare:workers';import { eq } from 'drizzle-orm';import { getDb } from '../../../../db';import { posts } from '../../../../db/schema';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const[row]=await getDb().select().from(posts).where(eq(posts.id,id)).limit(1);if(!row)return new Response('Not found',{status:404});const object=await env.FILES.get(row.imageKey);if(!object)return new Response('Not found',{status:404});return new Response(object.body,{headers:{'Content-Type':row.imageType,'Content-Disposition':`attachment; filename="skillshot-${id}"`,'X-Content-Type-Options':'nosniff'}})}
