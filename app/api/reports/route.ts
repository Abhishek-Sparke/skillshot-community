import { requirePrincipal } from '../../../lib/authz';
import { getReadyDb } from '../../../lib/db';
import { rateLimit } from '../../../lib/rate-limit';
const categories=new Set(['NSFW','HARASSMENT','HATE','VIOLENCE','SPAM','SCAM','COPYRIGHT','OTHER']);
export async function POST(request:Request){const auth=await requirePrincipal();if('error'in auth)return auth.error;if(!await rateLimit(`report:${auth.principal.id}`,8,3600))return Response.json({error:'Too many reports.'},{status:429});const body=await request.json();const type=String(body.targetType||'').toUpperCase(),target=String(body.targetId||''),category=String(body.category||'').toUpperCase();if(!['SKILLSHOT','COMMENT','PROFILE'].includes(type)||!target||!categories.has(category))return Response.json({error:'Invalid report'},{status:400});const sql=await getReadyDb();const found=type==='SKILLSHOT'?await sql.query(`SELECT user_id FROM posts WHERE id=$1`,[target]):type==='COMMENT'?await sql.query(`SELECT user_id FROM comments WHERE id=$1`,[target]):await sql.query(`SELECT id user_id FROM users WHERE lower(username)=lower($1)`,[target]);if(!found.length)return Response.json({error:'Item not found'},{status:404});if(found[0].user_id===auth.principal.id)return Response.json({error:'You cannot report your own content.'},{status:400});try{const id=crypto.randomUUID();await sql.query(`INSERT INTO reports(id,reporter_id,target_type,target_id,category,details)VALUES($1,$2,$3,$4,$5,$6)`,[id,auth.principal.id,type,target,category,String(body.details||'').slice(0,500)]);await sql.query(`INSERT INTO moderation_queue(id,source,target_type,target_id,creator_id,category,severity)VALUES($1,'REPORT',$2,$3,$4,$5,'BORDERLINE')`,[crypto.randomUUID(),type,target,found[0].user_id,category]);await sql.query(`INSERT INTO notifications(id,audience,type,title,body)VALUES($1,'STAFF','NEW_REPORT','New report awaiting review',$2)`,[crypto.randomUUID(),`${type}: ${category}`]);return Response.json({id},{status:201});}catch{return Response.json({error:'You already reported this item.'},{status:409});}}
